# 🍊 tripSeat - 揪團旅遊 Social Trip Platform

<!-- ![tripSeat Logo](https://image.pollinations.ai/prompt/tripSeatLOGO) -->

**tripSeat** 是一個專為「揪團旅遊」設計的社群平台。透過簡約現代的設計與強大的信任機制，讓志同道合的旅伴能夠輕鬆相遇、安心出遊。

## 🚀 技術棧 (Tech Stack)

- **前端 (Frontend)**: React (Vite) + Bootstrap 5
- **模擬後端 (Backend)**: json-server + json-server-auth (JWT Ready)
- **設計風格**: 橘黃色系、簡約插畫、Google Fonts (Inter/Roboto)

## 📂 專案結構 (Directory Structure)

```text
tripSeat/
├── backend/                # Mock 後端伺服器
│   ├── db.json             # 存放所有旅程、會員、心得資料的資料庫
│   ├── public/             # (選填) 存放 json-server 需要的圖片資源
│   └── package.json        # (建議) 獨立管理 backend 啟動腳本
├── docs/                   # 您目前的線稿、使用者故事等文件 (保持不變)
├── public/                 # 前端靜態資源 (Vite)
├── src/                    # React 前端程式碼
│   ├── api/                # 【新增】管理 axios 與 json-server 請求
│   │   ├── axiosInstance.js # 設定 Base URL (e.g., http://localhost:3001)
│   │   ├── tripApi.js      # 旅程相關請求 (GET /trips, POST /trips)
│   │   └── userApi.js      # 會員相關請求 (GET /users, PATCH /users)
│   ├── assets/             # 圖片、圖標
│   ├── components/         # 共用組件 (Navbar, Footer, Card)
│   ├── pages/              # 頁面組件 (Home, Explore, Member...)
│   ├── routes/             # 路由配置 (React Router)
│   ├── App.jsx
│   └── main.jsx
├── .gitignore
├── package.json            # 前端專案依賴與腳本
├── vite.config.js          # Vite 配置 (建議在此設定 Proxy)
└── README.md
```

# Git 使用規範

> 本專案採用「feature → dev → main」流程，  
> feature 為個人分支請自行維護。

## 分支說明

| 分支           | 說明                                           |
| -------------- | ---------------------------------------------- |
| `main`         | **主線**，穩定可執行版本（每周線上會議，或重大更新時開PR 審核更新） |
| `dev`          | **整合驗證支線**，個人開發支線統一自行整合進此分支                 |
| `feature/uno`  | UNO 個人開發分支                               |
| `feature/gwen` | Gwen 個人開發分支                              |
| `feature/cami` | Cami 個人開發分支                              |
| `feature/tou`  | Tou 個人開發分支                               |

---

## 分支使用原則

1. 所有開發一律在自己的 `feature/*` 分支進行  
2. 禁止直接在 `main`、`dev` 分支上進行開發
3. 上推個人分支前，請將最新`dev`狀態merge 進自己的 `feature/*` 確認無衝突後再上推個人分支
4. 請自行上推 `feature/*` 分支後自行合併至`dev`
5. `main` 分支合併時間為每周會議中，開立PR至少2人審核後更新

---

# GITHUB 基本操作流程

**uno為舉例用，請依照分支說明用自己的名字填寫**

## 1️⃣ 開發前（確認主線狀態）

**本步驟僅供把本機的 main、dev 都對齊遠端基準**

```bash
git checkout main
git pull origin main
git checkout dev
git pull origin dev
```

### 說明

1. 切換到 `main` 分支
2. 同步最新 `main` 分支
3. 切換到 `dev` 分支
4. 同步最新 `dev` 分支

---

## 2️⃣ 建立個人開發分支

**當要新建個人開發分支，請使用這個步驟，務必從dev開立**

```bash
git checkout dev
git pull origin dev
git checkout -b feature/uno
git push -u origin feature/uno
```

### 說明

1. 切換到 `dev` 分支
2. 同步最新 `dev` 分支
3. 新建並切換到個人分支
4. 推到 GitHub，告知你有一條全新分支

---

## 3️⃣ 每次開發刪除個人開發分支並重新建立(選用)

```bash
git checkout dev
git pull origin dev
git branch -d feature/uno
git push origin --delete feature/uno
git checkout -b feature/uno
git push -u origin feature/uno
```

1. 切換到 dev
2. 同步最新 dev
3. 刪除本機個人支線 ( 用小寫可能會有警告，可以改用 git branch -D feature/uno 徹底刪除 )
4. 刪除遠端個人支線
5. 建立新的個人支線 ( 務必在dev支線下執行，才會複製 dev 內容)
6. 推到 GitHub，告知你有一條全新分支

> ### 注意事項
>
> 若選擇每週刪除本機分支重新建立，請依以上步驟執行，
> 並且需要刪除GitHub上自己的feature分支
>
>```bash
>git push origin --delete feature/*
>```
>
> 然後才能上推自己的分支
>
> ```bash
> git push -u origin feature/*    
> ```
>
> 以設定 upstream，後續在該分支上更新程式碼，可直接使用 `git push` 推送。

---

## 4️⃣ 上傳個人分支並建立 PR（feature → dev）
  
```bash
git push origin feature/uno
```

如果在個人支線狀態下可以使用

```bash
git push 
```

## 說明

1. ❗ PUSH 完成請自行透過線上PR merge 進 `dev` 解衝突並完成更新
2. 本專案衝突處理以 **GitHub PR 線上解衝突** 為主
3. 上推個人分支前，**可選擇是否**將最新 `dev` merge 進自己的 `feature/*`

- 若熟悉衝突處理，可先行本機合併
- 若不確定，請直接上推，統一於 GitHub PR 解衝突

---

## 5 每周會議進行 `dev` > `main` 合併

1. 須有一人發起PR至少兩位成員審核
2. 合併完成後，請同步更新本機 main / dev 分支

## 常用git指令說明

|指令|說明|
|---|---|
|``` git init ```|創建環境|
|``` git add . ```|加入追蹤|
|``` git commit -m "說明這次做了什麼" ```|提交|
|``` git push -u origin branch-name ```|首次推送至GitHub|
|``` git push ```|後續於該分支推送|
|``` git checkout -b branch-name ```| 建立並切換分支|
|``` git checkout branch-name ```|切換到該分支|
|``` git branch -d branch-name ```|刪除本機分支|
|``` git reset ```|退回暫存區（staged）狀態，不會刪檔案內容|
|``` git checkout . ```|放棄所有尚未 commit 的變更|
|``` git pull origin branch-name ```|下載同步目前分支(會動檔案)|
|``` git fetch origin ```|下載遠端資訊（不會動檔案）|
|``` git status ```|查看目前分支狀態|
|``` git branch ```|有 * 的是目前所在分支|
|``` git branch -r ```|查看遠端分支|
|``` git merge --no-ff origin/branch-name ```|合併分支（保留紀錄）|
|``` git merge --abort ```|中止正在進行的 merge|

**branch-name 請更換為分支**

# 專案初始化（首次下載完後務必執行安裝）

### 安裝相依套件

```bash
npm install
```

- 讀取 package.json
- 依照 package-lock.json
- 自動產生 node_modules

### 啟動開發環境

```bash
npm run dev
```

- 啟動本地開發伺服器
- 依照終端機提示開啟瀏覽器即可進行開發

### 啟動開發環境&json-server - port 3001(concurrently套件)

```bash
npm run dev-con
```

- 啟動本地開發伺服器同時啟動json-server - port 3001

### Build 專案

```bash
npm run build
```

- 啟動本地開發伺服器
- 依照終端機提示開啟瀏覽器即可進行開發
  
### deploy git

```bash
npm run deploy
```

- 此指令僅用於將 build 後的成品 部署至 GitHub Pages（gh-pages 分支）
- 僅限專案管理人於 main 更新前或發佈時執行
- 一般開發成員 不得 使用此指令
- 注意：npm run deploy 只會更新 gh-pages，不會影響 main 或 dev。

### **語意化分類（推薦！）**

- `feat:` 新功能
- `fix:` 修 bug
- `refactor:` 重構
- `docs:` 文件/註解
- `style:` 格式排版
- `chore:` 雜項（升級依賴、調整建置設定）

```bash
feat: 完成場域選擇頁 UI 初稿
fix: 修正登入頁密碼驗證邏輯
refactor: 抽離 header 為共用元件
docs: 補充專案啟動流程說明

## 🛠️ Getting Started

Please refer to [06-backend-guide.md](docs/06-backend-guide.md) to start the Mock API.

---
*最後更新日2026/01/16 UNO*
