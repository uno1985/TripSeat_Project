# ===================================================================
# TripSeat 環境變數範例檔 (.env.example)
# 
# 使用說明：
# 1. 請將此檔案內容複製，並在根目錄建立兩份檔案：
#    - .env.local       (開發環境：連接本機 Json-Server)
#    - .env.production  (正式環境：連接 Render 線上 API)
# 2. 本檔案已列入 Git 追蹤，請勿在此填寫真實的 API Key 或機密資訊。
# ===================================================================

# 目前連線狀態 (例如: "LOCALHOST RUN" 或 "PRODUCTION LIVE")
VITE_STATUS=LOCALHOST_RUN

# API 根路徑 (開發建議: http://localhost:3001，正式建議: Render 網址)
VITE_API_BASE=http://localhost:3001

# API 鑰匙 (目前開發中，暫未啟用)
VITE_API_KEY=your_secret_key_here